# Copy to .env in THIS machine-local worktree; never commit real values.
# Load explicitly: uv run --env-file .env content-eval check-providers
TYPESAFE_API_KEY=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
# Choose the exact GPT-5.6 variant available in your account; no silent default.
OPENAI_MODEL=
